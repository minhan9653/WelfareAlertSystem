<UserControl x:Class="GyeotaeAdmin.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <StackPanel Margin="20" Orientation="Vertical">

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 0 0 10">
            <!-- 보고서 생성 버튼 -->
            <Button Content="📊 보고서 생성" Command="{Binding GenerateReportCommand}" Padding="10,5" Margin="0,0,10,0"/>

            <!-- 엑셀 내보내기 버튼 -->
            <Button Content="💾 엑셀로 내보내기" Command="{Binding ExportToExcelCommand}" Padding="10,5"/>
        </StackPanel>
        <!-- 참여율 통계 -->
        <TextBlock Text="📊 프로그램 참여율 통계" FontSize="18" FontWeight="Bold" Margin="0 10 0 5"/>
        <ListView ItemsSource="{Binding ProgramStatistics}" Height="200">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="5">
                        <TextBlock Text="{Binding ProgramName}" Width="auto" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ParticipationCount, StringFormat='참여자 수: {0}'}" Margin="10,0"/>
                        <TextBlock Text="{Binding ParticipationRate, StringFormat='참여율: {0:F1}%'}" DockPanel.Dock="Right"/>
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- 🤖 AI 추천 프로그램 요약 -->
        <TextBlock Text="🤖 AI 추천 프로그램 요약" FontSize="18" FontWeight="Bold" Margin="0 20 0 5"/>
        <ListView ItemsSource="{Binding RecommendationSummary}" Height="150">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="5">
                        <TextBlock Text="{Binding ProgramName}" Width="auto" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding FormattedScore, StringFormat='AI 관심도: {0}'}" DockPanel.Dock="Right"/>
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- ⚠️ 미참여자 수 통계 -->
        <TextBlock Text="⚠️ 미참여자 수 통계" FontSize="18" FontWeight="Bold" Margin="0 20 0 5"/>
        <ListView ItemsSource="{Binding NonParticipants}" Height="200">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="5">
                        <TextBlock Text="{Binding ProgramName}" Width="auto" FontWeight="Bold"/>
                        <TextBlock Text="{Binding NonParticipationRate, StringFormat='미참여율: {0:F1}%'}" DockPanel.Dock="Right"/>
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackPanel>
</UserControl>
