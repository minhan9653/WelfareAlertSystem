<Window x:Class="GyeotaeAdmin.Views.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GyeotaeAdmin.Views"
        xmlns:helpers="clr-namespace:GyeotaeAdmin.Helpers"
        mc:Ignorable="d"
        Title="관리자 로그인"
        Height="280" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#f4f6f8">

    <Border Margin="20" Padding="20" Background="White" CornerRadius="10" BorderBrush="#DDD" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="아이디" Grid.Row="0" Margin="0,0,0,5" FontWeight="Bold" FontSize="13"/>
            <TextBox Grid.Row="1" Height="30" Margin="0,0,0,10"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="비밀번호" Grid.Row="2" Margin="0,0,0,5" FontWeight="Bold" FontSize="13"/>
            <PasswordBox Grid.Row="3" Height="30" Margin="0,0,0,10"
                         helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay}"/>

            <Button Grid.Row="4"
                    Content="로그인"
                    Width="100" Height="35"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0"
                    Command="{Binding LoginCommand}"/>
        </Grid>
    </Border>
</Window>